# .goreleaser.yml
brews:
    name: hssh
    tap:
      owner: heply
      name: homebrew-tap
    folder: Formula
    description: "Heply CLI to simplify the management of SSH hosts"
    license: "MIT"
    dependencies:
      - name: fzf
